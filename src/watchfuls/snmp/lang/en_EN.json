{
    "pretty_name": "SNMP",
    "module_description": "Monitors SNMP-enabled devices. Define servers once, then attach any number of OID checks to each server. Supports SNMP v1, v2c and v3 (authPriv).",
    "labels": {
        "enabled": "Enabled",
        "threads": "Threads",
        "mib_dirs": "MIB directories",
        "github_token": "GitHub token",
        "label": "Label",
        "host": "Host / IP",
        "port": "Port",
        "version": "SNMP Version",
        "community": "Community",
        "timeout": "Timeout (s)",
        "retries": "Retries",
        "snmpv3_username": "Username",
        "snmpv3_auth_key": "Auth Key",
        "snmpv3_priv_key": "Privacy Key",
        "snmpv3_auth_protocol": "Auth Protocol",
        "snmpv3_priv_protocol": "Privacy Protocol",
        "snmpv3_level": "Identification type",
        "snmpv3_context": "Context",
        "snmpv3_engine_id": "Engine ID",
        "server": "Server",
        "oid": "OID",
        "operator": "Operator",
        "value": "Expected Value",
        "alert": "Alert threshold",
        "device_profiles": "Device profiles"
    },
    "hints": {
        "enabled": "Enable or disable this entry.",
        "threads": "Maximum number of OID checks to run in parallel.",
        "mib_dirs": "Comma-separated list of additional directories containing compiled Python MIB modules (output of mibdump.py / pysmi). Used to resolve OID names and types during discovery. The application data folder (snmp_mibs/compiled/) is always searched automatically — drop raw ASN.1 .mib files into snmp_mibs/raw/ for automatic compilation (requires pysmi). Leave empty to use only the built-in RFC/IETF MIBs.",
        "github_token": "Optional, and only for importing MIBs from GitHub. Without one GitHub allows 60 requests an hour, and a large repository — LibreNMS has some 400 vendor folders, one request each — runs out part way through. A read-only token (no scopes needed for public repositories) raises it to 5000. Stored encrypted.",
        "label": "Display name shown in the UI. Falls back to the item key if empty.",
        "host": "IP address or hostname of the SNMP device.",
        "port": "UDP port for SNMP queries (default 161).",
        "version": "SNMP protocol version.",
        "community": "Community string for SNMP v1/v2c authentication.",
        "timeout": "Seconds to wait for each SNMP response.",
        "retries": "Number of retries on timeout before declaring failure.",
        "snmpv3_username": "SNMP v3 security name (username).",
        "snmpv3_auth_key": "SNMP v3 authentication passphrase.",
        "snmpv3_priv_key": "SNMP v3 privacy (encryption) passphrase. Leave empty for authNoPriv.",
        "snmpv3_auth_protocol": "Hash algorithm for SNMP v3 authentication.",
        "snmpv3_priv_protocol": "Encryption algorithm for SNMP v3 privacy.",
        "snmpv3_level": "What the SNMP v3 session protects: the user's identity alone, authentication as well, or the conversation encrypted on top. It is what decides which keys are needed below.",
        "snmpv3_context": "SNMP v3 context the queries are addressed to. Many devices expose a VLAN or an instance per context; leave empty for the default one.",
        "snmpv3_engine_id": "The agent's engine ID, in hex. Only needed when the device does not advertise it and automatic discovery fails.",
        "server": "Key of the server entry (from the Servers section) to use for this check. Leave empty to use the first enabled server.",
        "oid": "OID to query in dotted-decimal notation (e.g. 1.3.6.1.2.1.1.1.0 for sysDescr). Use Discover to browse available OIDs.",
        "operator": "Comparison operator. Use 'any' to check reachability only.",
        "value": "Expected value for the comparison. Numeric operators (gt/lt/gte/lte) cast the OID value to float.",
        "alert": "Consecutive failures before triggering an alert.",
        "device_profiles": "What this device IS, in terms of what it can answer: a server, a switch and a NAS do not measure the same things. A profile is a list of OIDs with their unit and chart type, and several are assigned — the generic ones (MIB-II, interfaces) plus whatever belongs to the model. Empty: the device is not sampled, only its OID checks run."
    },
    "option_labels": {
        "snmpv3_level": {
            "noAuthNoPriv": "No authentication, no privacy",
            "authNoPriv": "Authentication, no privacy",
            "authPriv": "Authentication and privacy"
        }
    },
    "new_item_key_label": "Entry name or identifier:",
    "rename_item_prompt": "Enter new entry name or identifier:",
    "group_labels": {
        "connection": "Connection",
        "auth": "Authentication",
        "device": "Device"
    },
    "collections": {
        "servers": "Servers",
        "checks": "OID Checks"
    },
    "messages": {
        "snmp_error": "SNMP: {} 💥 {}",
        "snmp_no_host": "SNMP: {} ⚠ server not configured",
        "snmp_up": "SNMP: {} 🔼 [{}]",
        "snmp_down": "SNMP: {} 🔽 [{}]",
        "snmp_sampled": "SNMP: {} 📊 {} values",
        "snmp_sample_down": "SNMP: {} 💥 no data ({})"
    },
    "messages_vars": {
        "snmp_error": [
            "check",
            "error"
        ],
        "snmp_no_host": [
            "check"
        ],
        "snmp_up": [
            "check",
            "value"
        ],
        "snmp_down": [
            "check",
            "value"
        ],
        "snmp_sampled": [
            "device",
            "values"
        ],
        "snmp_sample_down": [
            "device",
            "error"
        ]
    }
}
