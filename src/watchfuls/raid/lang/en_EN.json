{
    "pretty_name": "RAID",
    "module_description": "Monitors RAID array health via /proc/mdstat on each bound host — locally or over the host's SSH connection. Alerts on degraded or recovering arrays.",
    "labels": {
        "enabled":     "Enabled",
        "threads":     "Threads",
        "timeout":     "Timeout (s)",
        "mdstat_path": "mdstat path",
        "label":       "Label"
    },
    "hints": {
        "enabled":     "Enable or disable this RAID check.",
        "threads":     "Number of hosts to check in parallel.",
        "timeout":     "Command/SSH timeout in seconds.",
        "mdstat_path": "Path to the mdstat file read on the host (default /proc/mdstat).",
        "label":       "Display name shown in the UI. Falls back to the item key if empty."
    },
    "new_item_key_label": "Check name or identifier:",
    "rename_item_prompt": "Enter new check name or identifier:",
    "collections": {"list": "RAID checks"}
}
