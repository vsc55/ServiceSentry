{
    "pretty_name": "Websites",
    "labels": {
        "enabled": "Enabled",
        "label":   "Label",
        "threads": "Threads",
        "url":     "URL",
        "code":    "HTTP Code",
        "timeout": "Timeout (s)"
    },
    "hints": {
        "enabled": "Enable or disable monitoring for this URL.",
        "label":   "Display name shown in the UI. Falls back to the item key if empty.",
        "threads": "Number of URLs to check in parallel.",
        "url":     "HTTP or HTTPS URL to check. If empty, the item key is used as the URL.",
        "code":    "Expected HTTP response code. An alert is triggered when the actual code differs from this value.",
        "timeout": "Request timeout in seconds. The check fails if the server does not respond within this time."
    },
    "collections": {"list": "URLs"}
}
