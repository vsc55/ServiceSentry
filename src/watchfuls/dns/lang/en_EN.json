{
    "pretty_name": "DNS Monitor",
    "labels": {
        "enabled":     "Enabled",
        "threads":     "Threads",
        "timeout":     "Timeout (s)",
        "host":        "Host",
        "record_type": "Record type",
        "expected":    "Expected value"
    },
    "hints": {
        "enabled":     "Enable or disable DNS monitoring for this entry.",
        "threads":     "Number of entries to check in parallel.",
        "timeout":     "Resolution timeout in seconds. Per-item value overrides the module-level default.",
        "host":        "Hostname to resolve. If empty, the item key is used.",
        "record_type": "DNS record type to query: A (IPv4), AAAA (IPv6), CNAME, MX, TXT, NS, PTR or SOA. Types other than A/AAAA require dnspython.",
        "expected":    "Optional value that must appear (case-insensitive substring match) in at least one resolved record. Leave empty to only check that the query returns results."
    },
    "option_labels": {
        "record_type": {
            "A":    "A — IPv4 address",
            "AAAA": "AAAA — IPv6 address",
            "CNAME": "CNAME — Canonical name",
            "MX":   "MX — Mail exchange",
            "TXT":  "TXT — Text record",
            "NS":   "NS — Name server",
            "PTR":  "PTR — Reverse lookup",
            "SOA":  "SOA — Start of authority"
        }
    },
    "new_item_key_label": "Host name or identifier:",
    "rename_item_prompt": "Enter new host name or identifier:",
    "collections": {"list": "Hosts"}
}
