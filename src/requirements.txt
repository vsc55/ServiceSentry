# ServiSesentry - Dependencias de producción
#
# Este fichero declara la INTENCIÓN (compatibilidad mínima con rangos >=). Lo que se
# INSTALA en Docker, CI y setup_env.ps1 es requirements.lock: versiones exactas, ya
# probadas y con hashes (integridad), para builds reproducibles. Regenerar tras cambiar
# una dependencia, con el venv de desarrollo activo:
#   1) pip-compile --strip-extras -o requirements.lock requirements.txt   (resuelve el árbol)
#   2) reconcilia las versiones a las probadas en el venv y corre la suite completa
#   3) pip-compile --generate-hashes --strip-extras -o requirements.lock <esas versiones>
#      (añade los hashes a las versiones ya reconciliadas)
# No edites requirements.lock a mano salvo para esa reconciliación.
requests>=2.28
paramiko>=5.0.0        # floor raised from 3.0: <5 allows SHA-1 in rsakey.py (CVE-2026-44405)
PyMySQL>=1.0
psycopg2-binary>=2.9
pymssql>=2.2
pymongo>=4.0
redis>=5.0
pymemcache>=4.0
psutil>=5.9
flask>=3.0
werkzeug>=3.0
# Imported directly (app.py: ChoiceLoader/FileSystemLoader); comes via Flask too.
jinja2>=3.0
pythonping>=1.1.4
cryptography>=50.0.0   # floor raised from 48.0.1: <50 lets a constrained CA be escaped with a
                       # wildcard SAN and blows up on duplicated self-signed chains
                       # (CVE-2026-69248 / -69249), both reachable through the ssl_cert module
dnspython>=2.3
# Optional: LDAP/Active Directory authentication
ldap3>=2.9
# Optional: OIDC/OAuth2 SSO authentication
authlib>=1.3
# Optional: SAML2 SSO authentication
python3-saml>=1.16
# Optional: Microsoft Teams integration — validates Teams SSO tokens (tab sign-in) and
# the Bot Framework JWT (bot messaging endpoint). Absent → those features self-disable.
PyJWT[crypto]>=2.8
# Optional: SNMP monitoring
pysnmp>=6.0
# Optional: SNMP MIB compilation (enables auto-compile of .mib files for SNMP discovery)
pysmi>=1.1
# Optional: importing the device-type catalogue (netbox-community/devicetype-library, CC0).
# Absent → the importer says so and the rest of the physical inventory works without it.
PyYAML>=6.0