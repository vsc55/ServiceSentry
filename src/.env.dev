# Entorno de desarrollo — cargado por los launchers de VS Code.
# No añadas credenciales reales aquí.

# Sin variables de entorno por defecto en modo dev;
# todo se lee de config.json (data/).
# Usa el launcher "🔒 Web (Env Vars)" para probar el sistema de env vars.
